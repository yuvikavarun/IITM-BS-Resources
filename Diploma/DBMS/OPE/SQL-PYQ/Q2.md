Write an SQL query to find the player IDs of the players whose name starts with 'K'

<img width="831" height="731" alt="flis" src="https://github.com/user-attachments/assets/981d72d5-6c7b-4499-8c81-52f683e891e0" />

Solution:
```
SELECT player_id
FROM players
WHERE name LIKE 'K%'
```
